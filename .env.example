PORT=8080
# =============================================================================
# Self-Learning Classifier - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# =============================================================================

# -----------------------------------------------------------------------------
# MySQL Database Configuration
# -----------------------------------------------------------------------------
# Root password for MySQL admin access
MYSQL_ROOT_PASSWORD=change_this_root_password

# Application database password
MYSQL_PASSWORD=change_this_db_password

# Database user (non-root)
DB_USER=slc

# Database name
DB_NAME=self-learning-classifier

# Database host (use 'db' for docker-compose, or IP/hostname for external DB)
DB_HOST=db

# Internal database port (default: 3306)
DB_PORT=3306

# External port to expose MySQL (for phpMyAdmin, DBeaver, etc.)
# Set to different port to avoid conflicts with local MySQL
DB_EXTERNAL_PORT=3307

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# Port where backend API will listen
BACKEND_PORT=8080

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------
# Port where frontend will be accessible
FRONTEND_PORT=3000

# Backend API URL for frontend
# Development: http://localhost:8080
VITE_API_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# HTTP header name for user identification
USER_ID_HEADER=X-User-ID

# Cookie name for anonymous users
ANON_COOKIE_NAME=slc_uid

# -----------------------------------------------------------------------------
# Production-specific (uncomment for production deployment)
# -----------------------------------------------------------------------------
# GIN_MODE=release
# TZ=UTC
# LOG_LEVEL=info

# -----------------------------------------------------------------------------
# SSL/TLS (uncomment if using HTTPS)
# -----------------------------------------------------------------------------
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem
DB_USER=db_user
DB_PASSWORD=db_password
DB_HOST=db_host
DB_PORT=db_port
DB_NAME=self-learning-classifier